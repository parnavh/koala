generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Config {
  guildId BigInt @id

  voice VoiceConfig?
}

model VoiceConfig {
  guildId      BigInt             @id @unique
  enabled      Boolean
  announce     Boolean
  announceMode VoiceAnnounceModes

  config Config @relation(fields: [guildId], references: [guildId], onDelete: Cascade)
}

model VoiceChannel {
  id      BigInt @id
  guildId BigInt
}

enum VoiceAnnounceModes {
  GLOBAL
  ENABLE
  DISABLE
}
